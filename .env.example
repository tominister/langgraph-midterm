# Example environment variables
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION=user_docs
EMBED_MODEL=all-MiniLM-L6-v2
EMBED_DIM=384
LLM_ENDPOINT=
LLM_API_KEY=
# Optional: select LLM provider. Supported: openai, groq (case-insensitive). If empty,
# the system will use a stubbed response.
LLM_PROVIDER=groq
# For Groq: set LLM_ENDPOINT to your model's inference URL (example only):
# https://api.groq.com/v1/models/<model-name>/invoke
# and set LLM_API_KEY to your Groq API key.
# Example:
# LLM_ENDPOINT=https://api.groq.com/v1/models/groq-2-small/invoke
# LLM_API_KEY=sk-...

# Klangoo: a simple NLP analyze endpoint. Set provider to 'klangoo' and optionally set
# LLM_ENDPOINT to a custom Klangoo analyze URL (default used below). Klangoo expects
# a POST body of {"text": "..."} and will return analysis.
# Example:
# LLM_PROVIDER=klangoo
# LLM_ENDPOINT=https://api.klangoo.com/v1/analyze
# LLM_API_KEY=sk-...
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
UPLOAD_DIR=./uploads
